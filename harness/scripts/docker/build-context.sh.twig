#!/bin/bash

function set_docker_build_context {
{% if @('docker.experimental.multiplatform_build.enabled') %}
    CONTEXT=$(docker buildx create --use)
    export CONTEXT
{% else %}
    :
{% endif %}
}

set_docker_build_context
