#!/bin/bash

function task_modules_after()
{
    {% if @('go.modules.after.steps') is empty %}
        :
    {% else %}
        {% for step in @('go.modules.after.steps') -%}
            {{ step|raw }}
        {% endfor %}
    {% endif %}
}
