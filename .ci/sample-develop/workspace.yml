workspace('ci-go-sample'):
  description: generated local workspace for ci-go-sample.
  harness: inviqa/go

attribute('app.build'): develop
attribute('module_name'): ci-go-sample

attributes:
  go:
    modules:
      after:
        steps:
          - echo "After go mod download!"
