  mysql:
    image: mysql:{{ @('services.mysql.version') }}
    labels:
      - traefik.enable=false
    environment:
{{ to_yaml(@('services.mysql.environment'), 2, 6) | raw }}
    networks:
      - private
    ports:
      - 3306
