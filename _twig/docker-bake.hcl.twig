variable "PLATFORMS" {
    default = [
{% for platform in @('docker.platforms.production') %}
        "{{platform}}",
{% endfor %}
    ]
}

group "default" {
    targets = [
        "app",
    ]
}

target "app" {
    dockerfile = ".my127ws/docker/image/app/Dockerfile"
    target = "prod"
    platforms = PLATFORMS
    tags = [
        "{{ @('docker.repository') }}:{{ @('docker.tagPrefix') }}{{ @('app.version') }}"
    ]
}
