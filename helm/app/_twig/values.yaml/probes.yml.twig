readinessProbe:
  tcpSocket:
    port: = @('app.health_port')
    initialDelaySeconds: 5
    periodSeconds: 10
livenessProbe:
  tcpSocket:
    port: = @('app.health_port')
    initialDelaySeconds: 10
    periodSeconds: 10
